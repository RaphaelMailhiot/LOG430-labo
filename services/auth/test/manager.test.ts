import { describe, it, expect } from '@jest/globals';

describe('ManagerAuthService', () => {
    it('devrait retourner true pour un test de base', () => {
        expect(true).toBe(true);
    });
});